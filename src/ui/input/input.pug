mixin field(props)
    -
        props = Object.assign({
            placeholder: '',
            name: '',
            type: 'text',
        }, props || {})

    label.field&attributes(attributes)
        .field__title= props.placeholder
        input.field__input(name=props.name, type=props.type, placeholder=props.placeholder)
        block
